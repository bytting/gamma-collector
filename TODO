Add buffering of messages in net_proc and make it resistant to temporary disconnects

Only allow one connected client

Add command line parameters needed to start up without networking

Finish systemd scripts and auto bootstrap

Make spec_proc independant to make it easier to add support for more detector types
