Only allow one connected client

Handle messages bigger than the pipe buffer size, or work around it

Store spectrums on disk. This can also be useful for working around the pipe buffer size

Add command line parameters needed to start up without networking

Finish documenting the code
